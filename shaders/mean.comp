#version 450

layout(local_size_x = 16, local_size_y = 16) in;

layout(binding = 0, rgba8) uniform readonly image2D inputImage;
layout(binding = 1, rgba8) uniform writeonly image2D outputImage;

// Shared memory for reduction within the workgroup (16x16 = 256 threads)
shared vec4 s_colors[256];

void main() {
    ivec2 inputSize = imageSize(inputImage);
    ivec2 outputSize = imageSize(outputImage);
    
    ivec2 myBlockPos = ivec2(gl_WorkGroupID.xy);
    ivec2 localCoord = ivec2(gl_LocalInvocationID.xy);
    uint threadIndex = gl_LocalInvocationIndex; // 0 to 255
    
    // Calculate global pixel coordinate for this thread
    ivec2 globalPos = myBlockPos * 16 + localCoord;
    
    // Load pixel color
    vec4 color = vec4(0.0);
    if (globalPos.x < inputSize.x && globalPos.y < inputSize.y) {
        color = imageLoad(inputImage, globalPos);
    }
    
    // Store in shared memory
    s_colors[threadIndex] = color;
    
    barrier();
    
    // Parallel reduction to compute sum
    for (uint s = 128; s > 0; s >>= 1) {
        if (threadIndex < s) {
            s_colors[threadIndex] += s_colors[threadIndex + s];
        }
        barrier();
    }
    
    // Thread 0 writes the result
    if (threadIndex == 0) {
        vec4 avgColor = s_colors[0] / 256.0;
        
        if (myBlockPos.x < outputSize.x && myBlockPos.y < outputSize.y) {
            imageStore(outputImage, myBlockPos, avgColor);
        }
    }
}



// // VERSION 2

// #version 450
// #extension GL_KHR_shader_subgroup_arithmetic : require

// layout(local_size_x = 16, local_size_y = 16) in;

// layout(binding = 0, rgba8) uniform readonly image2D inputImage;
// layout(binding = 1, rgba8) uniform writeonly image2D outputImage;

// // Shared memory to hold partial sums from each subgroup
// shared vec4 subgroupSums[8]; // 256 threads / 32 min subgroup size = 8

// void main() {
//     ivec2 pixelCoord = ivec2(gl_GlobalInvocationID.xy);
//     vec4 color = imageLoad(inputImage, pixelCoord);

//     // Step 1: SIMD-style sum within the subgroup
//     vec4 sum = subgroupAdd(color);

//     // Step 2: The "leader" of each subgroup writes to shared memory
//     if (subgroupElect()) {
//         subgroupSums[gl_SubgroupID] = sum;
//     }

//     // Ensure all subgroups have finished writing
//     memoryBarrierShared();
//     barrier();

//     // Step 3: Use the first subgroup to sum the partial results
//     if (gl_SubgroupID == 0) {
//         // Read from shared memory (only up to the number of subgroups actually used)
//         vec4 partialSum = (gl_SubgroupInvocationID < gl_NumSubgroups) 
//                           ? subgroupSums[gl_SubgroupInvocationID] 
//                           : vec4(0.0);
        
//         vec4 totalSum = subgroupAdd(partialSum);

//         // Step 4: Calculate mean and write to the output
//         if (subgroupElect()) {
//             vec4 meanColor = totalSum / (16.0 * 16.0);
//             // Write to the top-left coordinate of the 16x16 block in the output image
//             imageStore(outputImage, pixelCoord / 16, meanColor);
//         }
//     }
// }